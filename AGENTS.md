# AGENTS.md

Guidance for AI coding agents (and human contributors) working in this repository. This is a native macOS app built on the Glaze app platform — a two-process architecture (frontend + backend) that behaves similarly to Electron, but with its own SDK and constraints described below.

## What this app is

**Multi Monitor** — a menu bar + dashboard app that watches CI/CD, incidents, status, and observability activity across many accounts of many providers at once (GitHub Actions, Cloudflare Pages/Workers, Supabase, Netlify, Resend, Grafana, Heroku, Sentry, PagerDuty, Statuspage, Datadog, Honeycomb, PostHog, Better Stack), via a pluggable provider registry. The root route (`/`) is the Command Center summary; the detailed live grouped provider dashboard is preserved at `/dashboard`; provider-specific deep dives live at `/providers`. See `.glaze_memory/PROJECT-CONTEXT.md` for the full current-state snapshot and change history — read that first when picking up work here.

## Architecture

- **Frontend** (`renderer/`): React + Vite, rendered inside a native macOS WebView.
- **Backend** (`main/`): Node.js (`handlers/` = IPC surface, `services/` = business logic), calls native OS APIs (notifications, tray, encrypted storage, shell).
- Frontend and backend only talk over an IPC bridge. Renderer code calls `window.glazeAPI.*`, which is exposed via `renderer/preload.ts`'s `contextBridge`. Only `preload.ts` may import `ipcRenderer` directly — never do that from renderer components.

## Repo layout

```
main/            backend: handlers/ (IPC endpoints), services/ (logic, providers/ registry+adapters)
renderer/        frontend: main/ (dashboard app + components/hooks), settings/ (settings window)
package.json     npm deps go here
glaze.ts         app/window configuration
.glaze_memory/   PROJECT-CONTEXT.md (current state, always read first) + PROJECT-HISTORY.md (full log)
```

`.glaze/` (sibling build output directory, not shown above) is generated — never hand-edit it; change source under `main/`/`renderer/` or `glaze.ts` instead, and rebuild.

## Hard constraints

- **Never modify the framework SDK** (`@glaze/core`, resolved via path aliases) and never `npm install` it — treat it as read-only, vendored, and version-pinned by the platform.
- **Only use the SDK's public backend exports** (`dialog`, `shell`, `clipboard`, `Notification`, `Menu`, `Tray`, `safeStorage`, etc.). Don't reach into internal/native-bridge modules, IPC server internals, or anything not part of the public export surface — if an API isn't exported, treat it as unavailable rather than working around the boundary.
- **IPC security**: only `renderer/preload.ts` imports `ipcRenderer`; all other code uses `window.glazeAPI`. Sensitive capabilities (clipboard, opening external URLs, etc.) must be explicitly wired/enabled in preload rather than exposed wholesale.
- **Window surfaces**: never fake blur with CSS `backdrop-filter` / Tailwind `backdrop-blur-*` as a window background. Use native window vibrancy with `frame: true` for frosted panels/HUDs/popovers; only use `frame: false` for windows where the app itself draws every visible pixel (custom transparent shapes).
- **Secrets**: this app stores provider credentials via OS-level encrypted storage (`safeStorage`, see `main/services/token-store.ts`) — never persist raw tokens/secrets in plain JSON or localStorage.
- **Never ship mock or placeholder data** — wire real APIs or real user input; if something can't be verified without live credentials/network, say so rather than faking a response.

## Conventions in this repo

- **Adding a provider** = one adapter module `main/services/providers/<id>.ts` implementing `ProviderDefinition` (`fields`, `collectionAreas`, `validate`, `fetch`, plus optional observability hooks such as `fetchSignals`, `fetchIncidents`, `fetchMetricsSummary`, `getDeepLinks`, `fetchLogs`, `getDashboardQueryCapabilities`, `runDashboardQuery`, and backend-only `buildWorkspace` enrichment), registered in `providers/index.ts`, plus one icon/label entry in `renderer/main/components/provider-meta.tsx`. Everything else (add/edit dialog, collection diagnostics, dashboard filters, provider workspaces, apps cockpit, poller, aggregator) is data-driven off the registry — don't add new hardcoded per-provider branches elsewhere.
- **Styling**: Tailwind v4 utilities plus the design-system components already in use (semantic colors, `Text` variants, `rounded-*` roles). Don't hand-roll CSS files or reach for raw Tailwind palette colors.
- **Responsive routes**: main-window routes must remain usable at the app's 720px minimum width. Prefer shared route layout primitives, wrapping toolbars, compact More menus for secondary actions, horizontal section navigation below wide desktop, and table-local horizontal scrolling instead of page-level overflow.
- **User docs stay current**: every user-facing functionality change must update the in-app Help docs (`renderer/main/help-view.tsx`) in the same change, including setup steps, outputs, gotchas, troubleshooting, and any changed user-visible behavior. If the change adds or changes provider setup fields, keep the docs data-driven from provider registry metadata rather than duplicating secrets or token values.
- **Surgical edits**: every changed line should trace back to the requested change. Don't refactor unrelated working code or add comments to code you didn't touch.
- **Minimize round trips**: read every file you need for a change up front and batch independent edits; only sequence calls when one genuinely depends on a prior result's output.
- **Project memory discipline (mandatory, every change)**: after EVERY completed change, no matter how small (a color tweak, a one-line fix, a config change), update `.glaze_memory/PROJECT-CONTEXT.md` before considering the task done — overwrite the relevant `Current State` entries in place (it's a snapshot, not a log) and prepend a new `Recent History` entry (newest first). `PROJECT-HISTORY.md` is the append-only full log; it is auto-mirrored from `Recent History` by the system, so never write to it directly — just keep `PROJECT-CONTEXT.md` current and the history follows. This is what lets a fresh session re-ground without prior chat context.

## Commands

- Install deps: `npm install --include=dev` from the repo root — a plain `npm install` under a production `NODE_ENV` will prune devDependencies needed for local builds.
- Run the app: `npm run dev` builds and launches it as a native macOS app (full frontend+backend, hot-reloading renderer). `npm run dev:renderer` runs just the renderer in a browser-based dev server for fast UI iteration when you don't need the native backend/IPC.
- Production bundle: `npm run build`.
- Static validation: `npm run test:contracts && npm run type-check && npm run lint`; `npm run test:contracts` is a credential-free source check for provider registration, IPC/security boundaries, dashboard storage/linking, and shared filter/date-range wiring; `npm run format` to auto-format.
- All of the above shell out to the platform's own CLI (`glaze.ts`) — don't bypass it by invoking `vite`/`tsc`/`eslint` directly, since it wires in platform-specific config.

## Current data & IPC surface

- **Storage**: `userData/accounts.json` (accounts + project groups, no secrets; also used as account-derived group fallback for older retained history rows missing `groupId`), `userData/tokens.bin.json` (encrypted secrets), `userData/settings.json` (poll interval + notification flags + local history retention + digest + multiple global/scoped maintenance windows), `userData/history.json` (rolling samples/events/check latency pruned by configured retention, including per-account status plus alert/open-incident counts used by local dashboard panels, provider workspaces, and alert-rule tuning context, no secrets), `userData/local-incidents.json` (local incident lifecycle, no secrets), `userData/service-metadata.json` (local service owner/tier/link/dependency annotations used in Apps/Incidents/alert payloads, included in portable setup, no secrets), `userData/dashboards.json`/`checks.json`/`rules.json`/`channels.json` (portable setup metadata including non-secret dashboard variables for group/provider/account/check/owner/tier/dependency defaults, optional panel-level refresh/range overrides, non-secret rule target channel ids, and alert metrics for failure rate, check latency, check down, and open incidents, no secrets); per-tab dashboard/filter state including `accounts.filters.v1`, `providerWorkspace.filters.v1`, `notificationChannels.filters.v1`, saved filter preset arrays plus pinned default preset ids, and transient one-shot navigation/action payloads such as `apps.select.v1`, `accounts.verify.v1`, `alerts.select.v1`, `incidents.drilldown.v1`, `timeline.drilldown.v1`, `uptime.drilldown.v1`, and `dashboard.item.select.v1` in renderer localStorage. The Command Center route uses existing renderer queries and one-shot handoff payloads only; it adds no backend storage or IPC.
- **IPC channels**: `app:getInfo/getProjectPath`; `providers:list/listWorkspaceCapabilities/getWorkspaceOverview/runWorkspaceQuery/exportWorkspace` (provider metadata plus read-only provider workspace charts/resources/evidence/alert templates and secret-free CSV/JSON export from current snapshot + retained history; no tokens returned); `groups:list`; `accounts:list/add/update/remove/test/exportSetup/importSetup`; `setup:export/import` (portable setup, no secrets, remaps dashboard panel sources/variables and preserves intentionally empty dashboards); `diagnostics:listAccounts/runAccount` (account health plus non-secret collection-area and dashboard capability support/counts/names); `verification:run` (user-triggered smoke verification with dashboard default examples, optional channel delivery tests, no secrets returned); `services:listMetadata/saveMetadata/deleteMetadata`; `channels:list/save/delete/test` (notification-channel metadata only; webhook URLs stay encrypted); `monitor:getSnapshot/refresh/getSettings/updateSettings/getStatus/getItemLogs`; `monitor:openExternal`; `investigation:getContext` (non-secret current snapshot + retained-history + service metadata evidence for an item/event/manual scope); `window:openSettings/closeSettings`; `history:getSeries/getEvents/getStats/clear/prune` (relative or custom date ranges; series scoped by account/group/provider; events filterable by account/group/provider/status/severity/category/type; group filters fall back to current local account group metadata for retained rows missing `groupId`; retained-history metadata including file size; clear removes samples/events/check samples but keeps SLO definitions; prune applies current retention to disk); `history:listSlos/saveSlo/deleteSlo/getSloStatus`; `history:export` (CSV/JSON events or samples, optionally with the same retained-history date/scope/event filters); `checks:list/save/delete/getLatencySeries` (relative or custom date range for latency); `rules:list/save/delete/getState/preview/testDelivery` for alert-rule CRUD/state plus user-triggered evaluation/testing; `triage:list/acknowledge/silence/clear`; `localIncidents:list/save/updateStatus/delete/export`; `dashboards:list/save/delete/export/import/listCapabilities/runPanel` including persisted dashboard variables for local panels, Grafana alerts plus configured-only datasource health/Loki/Tempo/Prometheus live capabilities, GitHub workflow run panels, Cloudflare deploy panels, Netlify deploy panels, Resend domain/broadcast panels, Heroku release panels, Sentry issue search panels, PagerDuty incident panels, Statuspage incident/component panels, Datadog monitor panels, Honeycomb trigger/SLO panels, and local timeseries annotations from retained history through declared provider capabilities; legacy `grafana:getOverview/runLogPreset/runTracePreset/updateObservabilityConfig`; pushed events `monitor:snapshot`, `monitor:accountError`, `monitor:pollingState`, `settings:monitor-changed`.
- Full detail (key files, components in use, integrations, known pitfalls) lives in `.glaze_memory/PROJECT-CONTEXT.md` — keep this file and that one in sync when either changes.
